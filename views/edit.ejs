<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Edit Post</title>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body class="bg-gray-900 text-white min-h-screen flex items-center justify-center">
    <div class="w-full max-w-lg p-8 rounded-xl shadow-lg bg-gray-800">
      <!-- Logout Button -->
      <div class="flex justify-end mb-6">
        <a
          href="/logout"
          class="text-sm text-red-500 hover:text-red-400 font-semibold"
        >
          Logout
        </a>
      </div>

      <!-- Page Title -->
      <h5 class="text-3xl font-semibold text-center mb-6 text-gray-100">Edit Your Post</h5>

      <!-- Edit Post Form -->
      <form method="post" action="/update/<%= post._id %>" class="space-y-6">
        <!-- Textarea -->
        <textarea
          placeholder="Write your post here..."
          class="w-full p-4 bg-gray-700 border-2 border-gray-600 rounded-lg text-sm text-gray-300 focus:outline-none focus:ring-2 focus:ring-blue-400 focus:border-blue-500 resize-none"
          name="content"
          rows="4"
        ><%= post.content %></textarea>

        <!-- Submit Button -->
        <input
          type="submit"
          value="Update Post"
          class="w-full py-3 bg-gradient-to-r from-teal-500 to-pink-500 text-white font-semibold rounded-lg shadow-md hover:bg-teal-600 transition-all duration-300 cursor-pointer"
        />
      </form>

      <!-- Back Button (optional) -->
      <div class="mt-4 text-center">
        <a
          href="/profile"
          class="text-sm text-gray-400 hover:text-gray-300 font-medium"
        >
          ← Back to Profile
        </a>
      </div>
    </div>
  </body>
</html>
